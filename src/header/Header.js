import React, { Component } from 'react';

export default class Header extends Component {
  render() {
    return (
      <header>
        <h1>Escape from <em>Toy Factory</em></h1>
      </header>
    );
  }
}